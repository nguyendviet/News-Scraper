<div class="container">
    {{#each article}}
        <div class="card">
            <h3 class="card-header">{{this.title}}</h3>
            <div class="row">
                <div class="col-md-8 card-block">
                    <p class="card-text">{{this.summary}}</p>
                    <a href="http://www.bbc.com{{this.link}}" target="_blank" class="card-link">Read more</a>
                </div>
                <div class="col-md-4 align-self-end text-right">
                    <button class="btn btn-success m-2 btn-save" data-id="{{this._id}}">Save Article</button>
                </div>
            </div>
        </div>
    {{/each}}
</div>
